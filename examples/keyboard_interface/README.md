# Keyboard interface example

This example illustrates how to increment a counter using keyboard commands.

To compile the code, this example uses a Makefile.
You can compile the code by running `make` in this directory.

To execute the application, use the command `./test` 

To halt the program execution, press `CTRL + C`.